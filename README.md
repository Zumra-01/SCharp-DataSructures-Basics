# C# Data Structures: ArrayList Example

This project demonstrates the usage of **Non-Generic Collections** in C#.
Specifically, it focuses on the `ArrayList` class found in the `System.Collections` namespace.

## 🚀 What I Learned
* How to initialize an `ArrayList`.
* Storing different data types (string, integer) in a single collection.
* Iterating through collections using `foreach` loops.
* Understanding the concept of **Boxing** (converting value types to object).

## 💻 Tech Stack
* C#
* .NET Core / .NET Framework
* Visual Studio 2022
